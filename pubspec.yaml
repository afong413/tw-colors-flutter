name: tw_colors
description: "Tailwind CSS default color palettes as dart:ui Color constants, for Tailwind v2, v3, and v4."
version: 1.0.0
homepage: https://github.com/afong413/tw-colors-flutter
repository: https://github.com/afong413/tw-colors-flutter

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
